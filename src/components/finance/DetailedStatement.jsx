import React from 'react';
import DetailedStatementDialog from './DetailedStatementDialog';

const DetailedStatement = (props) => {
  return <DetailedStatementDialog {...props} />;
};

export default DetailedStatement;
