import React from 'react';
import SimpleStatementDialog from './SimpleStatementDialog';

const SimpleStatement = (props) => {
  return <SimpleStatementDialog {...props} />;
};

export default SimpleStatement;